anthropic>=0.40.0
openai>=1.40.0
python-dotenv>=1.0.0
mcp[cli]>=1.28.0,<2.0

# --- Assignment 2 ---
# Tracing (§2.1). Pinned to the v4 line: v4 is a platform migration with
# different SDK idioms than v2/v3, so the major is locked deliberately.
langfuse>=4.14.3,<5.0
# pgvector retrieval (§2.6.2)
psycopg[binary]>=3.3.4,<4.0
pgvector>=0.5.0,<1.0
# HTTP wrapper so the ALB has a target to health-check and curl (§2.6.1)
fastapi>=0.141.1,<1.0
uvicorn[standard]>=0.52.1,<1.0
